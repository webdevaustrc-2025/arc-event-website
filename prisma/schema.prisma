generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            Int            @id @default(autoincrement())
  name          String
  email         String         @unique
  passwordHash  String?
  role          String         @default("user")
  avatarUrl     String?
  createdAt     DateTime       @default(now())
  emailVerified DateTime?
  department    String?
  phone         String?
  studentId     String?
  university    String?
  accounts      Account[]
  certificates  Certificate[]
  leaderboard   Leaderboard[]
  registrations Registration[]
  sessions      Session[]
}

model Account {
  id                String  @id @default(cuid())
  userId            Int
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       Int
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model Segment {
  id             Int            @id @default(autoincrement())
  name           String
  description    String
  rules          String
  prizePool      String
  status         String         @default("active")
  imageUrl       String?
  category       String         @default("General")
  deadline       String         @default("TBA")
  difficulty     String         @default("Medium")
  fee            String         @default("TBA")
  highlights     String[]       @default([])
  location       String         @default("TBA")
  ruleBookUrl    String?
  scheduleText   String         @default("TBA")
  teamSize       String         @default("TBA")
  type           String         @default("Team")
  resultDeclared Boolean        @default(false)
  certificates   Certificate[]
  Leaderboard    Leaderboard[]
  registrations  Registration[]
  schedule       Schedule[]
}

model Registration {
  id            Int      @id @default(autoincrement())
  userId        Int
  segmentId     Int
  teamName      String
  status        String   @default("pending")
  paymentStatus String   @default("unpaid")
  qrToken       String   @unique
  createdAt     DateTime @default(now())
  segment       Segment  @relation(fields: [segmentId], references: [id], onDelete: Cascade)
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Schedule {
  id           Int      @id @default(autoincrement())
  segmentId    Int?
  title        String
  startTime    DateTime
  endTime      DateTime
  venue        String
  displayOrder Int      @default(0)
  segment      Segment? @relation(fields: [segmentId], references: [id])
}

model Sponsor {
  id           Int      @id @default(autoincrement())
  name         String
  logoUrl      String
  websiteUrl   String?
  displayOrder Int      @default(0)
  category     String   @default("Gold Sponsor")
  createdAt    DateTime @default(now())
  updatedAt    DateTime @default(now()) @updatedAt
}

model FAQ {
  id           Int    @id @default(autoincrement())
  question     String
  answer       String
  displayOrder Int    @default(0)
}

model PastEvent {
  id          Int      @id @default(autoincrement())
  name        String
  date        DateTime
  description String
  imageUrl    String?
}

model Certificate {
  id        Int      @id @default(autoincrement())
  userId    Int
  segmentId Int
  fileUrl   String
  issuedAt  DateTime @default(now())
  segment   Segment  @relation(fields: [segmentId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Leaderboard {
  id        Int      @id @default(autoincrement())
  userId    Int
  points    Int
  rank      Int
  segmentId Int?
  Segment   Segment? @relation(fields: [segmentId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Setting {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}

model Activity {
  id    Int      @id @default(autoincrement())
  title String
  desc  String
  time  DateTime @default(now())
  icon  String
  color String
}

model Announcement {
  id        Int      @id @default(autoincrement())
  title     String
  message   String
  icon      String   @default("Bell")
  color     String   @default("#588157")
  isNew     Boolean  @default(true)
  createdAt DateTime @default(now())
}

model Review {
  id           Int      @id @default(autoincrement())
  name         String
  team         String
  quote        String
  displayOrder Int      @default(0)
  createdAt    DateTime @default(now())
}

model AboutHero {
  id          Int      @id @default(autoincrement())
  heading     String
  description String
  imageUrl    String?
  updatedAt   DateTime @updatedAt
}

model AboutAustrc {
  id        Int      @id @default(autoincrement())
  title     String   @default("Where Bangladesh's Best Engineers Compete.")
  content   String
  imageUrl  String?
  updatedAt DateTime @updatedAt
}

model AboutMissionVision {
  id                 Int      @id @default(autoincrement())
  missionTitle       String   @default("Our Mission")
  missionDescription String
  visionTitle        String   @default("Our Vision")
  visionDescription  String
  updatedAt          DateTime @updatedAt
}

model AboutGallery {
  id           Int      @id @default(autoincrement())
  imageUrl     String
  title        String
  description  String
  displayOrder Int      @default(0)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model AboutWhatWeDo {
  id           Int      @id @default(autoincrement())
  title        String
  description  String
  icon         String   @default("HelpCircle")
  displayOrder Int      @default(0)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model AboutEvent {
  id           Int      @id @default(autoincrement())
  title        String
  tag          String   @default("Championship")
  badge        String   @default("Achievement")
  description  String
  date         String
  location     String
  imageUrl     String?
  displayOrder Int      @default(0)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
