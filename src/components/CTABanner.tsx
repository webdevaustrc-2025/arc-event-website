"use client";
import React from 'react';
import { motion } from 'motion/react';

const stats = [
  { number: '12', label: 'Competition Segments', accent: '#344e41', offset: 'translate-x-0' },
  { number: '500+', label: 'Student Participants', accent: '#3a5a40', offset: 'md:translate-x-4' },
  { number: '3', label: 'Days of Competition', accent: '#588157', offset: 'md:translate-x-2' }
];

const universities = ['BUET', 'KUET', 'RUET', 'SUST', 'DIU'];

const avatarInitials = ['AR', 'SI', 'FA', 'NJ', 'RH'];

export const CTABanner = () => {
  return (
    null
  );
};
